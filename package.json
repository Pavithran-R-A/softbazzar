{
  "name": "softbazzar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --experimental-loader ./scripts/asset-loader.mjs --test",
    "build": "vite build && node --experimental-loader ./scripts/asset-loader.mjs scripts/generate-seo.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "8.1.0"
  },
  "dependencies": {
    "@vercel/analytics": "2.0.1"
  }
}
